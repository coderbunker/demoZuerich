import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold' 
    },
    nav: {
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold',
        top: -2.5
    }
  });